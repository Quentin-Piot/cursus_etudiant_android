<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.utt.if26.projet_final_if26">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_utt"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_utt_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.cursus.CursusActivity" />
        <activity android:name=".views.etudiant.EtudiantActivity" />
        <activity android:name=".views.homepage.AddEtudiantActivity" />
        <activity android:name=".views.cursus.AddSemestreActivity" />
        <activity android:name=".views.semestre.EditSemestreActivity" />
        <activity android:name=".views.homepage.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>